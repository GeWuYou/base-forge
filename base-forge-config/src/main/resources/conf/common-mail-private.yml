spring:
  # 邮件配置
  mail:
    host: smtp.163.com
    # 邮箱发送地址
    username: gewuyou1126@163.com
    # 邮箱密码
    password: YTZQMQAACTROTVQQ
    default-encoding: UTF-8
    protocol: smtp
    port: 465
    properties:
      mail:
        smtp:
          auth: true
          socketFactory:
            class: javax.net.ssl.SSLSocketFactory
            port: 465
          ssl:
            enable: true
          starttls:
            enable: true
            required: true
      # 发信人
      from: GeWuYou
  thymeleaf:
    #prefix：指定模板所在的目录
    prefix: classpath:/templates/
    #check-template-location: 检查模板路径是否存在
    check-template-location: true
    #cache: 是否缓存，开发模式下设置为false，避免改了模板还要重启服务器，线上设置为true，可以提高性能。
    cache: false
    suffix: .html
    mode: HTML5



